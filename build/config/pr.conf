# Config file when Pull Rerquest is offered
CICD_BUILD_ENABLED="1"
CICD_TAGS_ID="${GIT_COMMIT_SHORT}" # Can be hash only!!

CICD_DEPLOY_ENABLED="0"
CICD_TAGS_DEPLOY_ENVIRONMENT="${CICD_TAGS_PR_ENV}" # Managed in generic.conf
